# Physical Manifest Template
# Defines the physical backends (data sources) for ADP curation
#
# Copy this file as physical.yaml and customize for your environment.

version: "1.0.0"

backends:
  # RDBMS Backend Example (PostgreSQL)
  # - id: "my_database"
  #   type: "RDBMS"
  #   provider: "postgresql"       # required: implementation variant (e.g. postgresql, mysql)
  #   config:
  #     type: "RDBMS"
  #     uri: "postgresql://localhost:5432/mydb"
  #     schema: "public"
  #   credentials:
  #     type: "env"
  #     key: "DB_PASSWORD"

  # Vector Backend Example (Pinecone)
  # - id: "my_vectors"
  #   type: "VECTOR"
  #   provider: "pinecone"         # required: implementation variant (e.g. pinecone, weaviate)
  #   config:
  #     type: "VECTOR"
  #     provider: "PINECONE"
  #     indexName: "my-index"
  #     endpoint: "https://api.pinecone.io"
  #   credentials:
  #     type: "secret"
  #     manager: "AWS_SECRETS_MANAGER"
  #     key: "production/pinecone-api-key"

  # S3 Backend Example
  # - id: "my_storage"
  #   type: "S3"
  #   provider: "s3"               # required: implementation variant (e.g. s3, minio)
  #   config:
  #     type: "S3"
  #     uri: "s3://my-bucket/"
  #     region: "us-east-1"
